interface:
  display_name: "Markdown to HTML"
  short_description: "Single-page HTML from Markdown with MathJax."
  default_prompt: "Convert one or more Markdown files to a single standalone HTML page with math support and optional title."
